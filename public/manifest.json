{
  "manifest_version": 3,
  "name": "Inject JS on Click",
  "version": "1.0",
  "description": "Injects JavaScript into the current page when the extension icon is clicked.",
  "permissions": ["scripting", "activeTab"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "jira-lite-ext-sw.js"
  },
  "action": {
    "default_title": "Inject JS"
  }
}
